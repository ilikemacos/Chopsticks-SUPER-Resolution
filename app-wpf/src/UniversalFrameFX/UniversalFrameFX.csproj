<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <AssemblyName>UniversalFrameFX</AssemblyName>
    <RootNamespace>UniversalFrameFX</RootNamespace>
    <Version>0.3.0</Version>
    <Product>Universal FrameFX</Product>
    <Company>Universal FrameFX (open source)</Company>
    <Description>A unified, honest interface for configuring AMD FSR, Intel XeSS and frame generation.</Description>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>
  </PropertyGroup>

  <ItemGroup>
    <!-- CSR's verified upscaler core. net8.0 with no Windows dependency, so the
         maths is tested on Linux CI; see csr/tests. -->
    <ProjectReference Include="../../../csr/src/Csr.Core/Csr.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.Management" Version="8.0.0" />
    <PackageReference Include="Microsoft.Win32.Registry" Version="5.0.0" />
  </ItemGroup>

</Project>
